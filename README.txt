ascii <chars> - Prints out the binary values for the ascii characters.
allascii - prints out the binary values for every ascii character with a decimal value from 0 to 127.

To compile the binaries run "make compile" and to install them run "make install"
